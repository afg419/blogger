<h1>Articles:</h1>
<ul>
  <% @articles.each do |article| %>
  <li><%=link_to(article.title, article_path(article), class: 'article_in_index')%></li>
  <% end %>
</ul>
<br>
<%= link_to("Create new Article", new_article_path, class: 'create_article') %>
